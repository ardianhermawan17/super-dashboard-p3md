# graphify ignore rules (same syntax as .gitignore, applied on top of it)
# Keep the graph about OUR code and docs, not vendored or generated files.

# dependencies and build output
node_modules/
.next/
out/
coverage/
bun.lock
*.lock

# generated by our own tooling
graphify-out/
obsidian-out/
.hermes.md
.ecc/
agent-history/entries/
src/lib/supabase/database.types.ts

# vendored shadcn/Base UI primitives: they would dominate the "god nodes" ranking
src/components/ui/

# nested layout: skill copies, secrets, lockfiles, template UI/assets
**/.agents/
**/.claude/
**/.hermes/
**/.zcode/
backend-architecture/.env
**/package-lock.json
frontend-architecture/src/components/ui/
frontend-architecture/public/

# static assets and local Supabase state
public/
supabase/.temp/
supabase/.branches/

# first pass: P3MD docs only; stock Kiranism template + backend scaffold excluded (remove to map them)
/frontend-architecture/
backend-architecture/

!docs/backend-architecture/
!docs/frontend-architecture/

# json config/schema files produce key-name noise in the AST pass
package.json
agent-history/schema.json

# archived monolith docs (superseded by the layer folders)
docs/_archive/
