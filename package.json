{
  "scripts": {
    "frontend-dev": "cd frontend-architecture && bun run",
    "obsidian:sync": "bun scripts/obsidian-sync.ts",
    "obsidian:watch": "bun scripts/obsidian-sync.ts --watch",
    "agent:context": "bun scripts/agent-context.ts",
    "agent:check": "bun scripts/obsidian-sync.ts --check && bun scripts/agent-context.ts --check",
    "agent:evaluate": "bun agent-evaluate/observability/report.ts",
    "agent:evaluate:test": "bun test agent-evaluate",
    "db:types": "supabase gen types typescript --local > frontend-architecture/src/lib/supabase/database.types.ts",
    "db:reset": "supabase db reset",
    "prepare": "husky"
  },
  "dependencies": {},
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "husky": "^9.1.7"
  }
}
